<h1>Scratchoff Campaigns</h1>


<ul>
	<% @admins.each do |admin| %>
		<a href="<%= admin_path(admin) %>">
			<li>
				<%= image_tag admin.logo_url.to_s, :title => admin.name, :class => "logo" %> <%= admin.name %>
			</li>
		</a>
	<% end %>
</ul>
